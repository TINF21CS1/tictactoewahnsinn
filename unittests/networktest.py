from unittest import TestCase

# Due to weird behaviour of the asyncronous code, the test will be implemented later
class TryTesting(TestCase):
    def test_always_passes(self):
        self.assertTrue(True)
